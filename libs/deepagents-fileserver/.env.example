# DeepAgents FileServer Environment Configuration
# Copy this file to .env and customize for your deployment

# API Key for FastAPI server (optional, auto-generated if not set)
# Generate a secure key with: openssl rand -base64 32
FILESERVER_API_KEY=your-secret-api-key-here

# Rate limiting configuration
# Maximum number of requests per window
FILESERVER_RATE_LIMIT_REQUESTS=100

# Rate limit window in seconds
FILESERVER_RATE_LIMIT_WINDOW=60

# Server configuration (if needed for custom deployments)
# FILESERVER_HOST=0.0.0.0
# FILESERVER_PORT=8080
# FILESERVER_ROOT_DIR=/data
